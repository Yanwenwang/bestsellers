const actionTypes = {
    LIST: {
        GET: {
            CALL: 'LIST.GET.CALL',
            SUCCESS: 'LIST.GET.SUCCESS',
            FAIL: 'LIST.GET.FAIL'
        }
    }    
};

export default actionTypes;